import { SpaceToUnderScorePipe } from './space-to-under-score.pipe';

describe('SpaceToUnderScorePipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceToUnderScorePipe();
    expect(pipe).toBeTruthy();
  });
});
